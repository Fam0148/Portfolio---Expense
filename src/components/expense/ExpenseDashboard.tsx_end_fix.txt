                {monthTxs.length === 0 && (
                  <div className="py-20 text-center text-gray-400 font-bold text-sm bg-gray-50/20 rounded-lg border border-dashed border-gray-200 mt-4">
                    No transactions this month.
                  </div>
                )}
              </div>
            </motion.div>
          </div>
        </div>
      </div>

      {/* PRINT-ONLY STATEMENT TEMPLATE */}
      <div id="expense-statement-print" className="hidden print:block fixed inset-0 bg-white z-[9999] overflow-auto p-12 text-[#171717]">
        <style dangerouslySetInnerHTML={{ __html: `
          @media print {
            body * { visibility: hidden; }
            #expense-statement-print, #expense-statement-print * { visibility: visible; }
            #expense-statement-print { 
              position: absolute !important; 
              left: 0 !important; 
              top: 0 !important; 
              width: 100% !important; 
              height: auto !important;
              padding: 40px !important;
              background: white !important;
              display: block !important;
            }
            @page { margin: 1cm; size: auto; }
          }
        `}} />
        
        <header className="border-b-2 border-gray-100 pb-8 mb-10 flex justify-between items-end">
          <div>
            <h1 className="text-4xl font-serif font-black tracking-tighter uppercase mb-2">Monthly Expenditure Statement</h1>
            <p className="text-sm font-bold text-gray-400 tracking-widest uppercase">{viewDate.toLocaleDateString('en-IN', { month: 'long', year: 'numeric' })} — {userName}'s Dashboard</p>
          </div>
          <div className="text-right">
            <span className="text-[10px] font-black text-emerald-600 uppercase tracking-[0.2em] block mb-1">Generated Tracking ID</span>
            <span className="text-xs font-mono font-bold bg-gray-50 px-2 py-1 rounded text-red-500 uppercase tracking-widest">#{Math.random().toString(36).substring(7).toUpperCase()}</span>
          </div>
        </header>

        <section className="grid grid-cols-2 gap-12 mb-12">
          <div className="space-y-6">
            <div>
              <h2 className="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-3 border-l-2 border-[#171717] pl-3">Income Summary</h2>
              <div className="space-y-2">
                <div className="flex justify-between items-center text-sm">
                  <span className="font-medium text-gray-500">Monthly Salary (Primary)</span>
                  <span className="font-bold">₹{Number(income.salary || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-sm">
                  <span className="font-medium text-gray-500">Side Hustle (Secondary)</span>
                  <span className="font-bold">₹{Number(income.sideHustle || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-lg font-bold border-t border-gray-100 pt-2 mt-2">
                  <span>Total Revenue</span>
                  <span>₹{totalIncome.toLocaleString('en-IN')}</span>
                </div>
              </div>
            </div>
          </div>
          <div className="space-y-6">
            <div>
              <h2 className="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-3 border-l-2 border-red-500 pl-3">Mandatory Fixed Expenses</h2>
              <div className="grid grid-cols-2 gap-x-8 gap-y-2">
                <div className="flex justify-between items-center text-xs">
                  <span className="text-gray-500">House Rent</span>
                  <span className="font-bold text-gray-700">₹{Number(fixedExpenses.rent || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-xs">
                  <span className="text-gray-500">Cook Cost</span>
                  <span className="font-bold text-gray-700">₹{Number(fixedExpenses.cook || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-xs">
                  <span className="text-gray-500">Travel Allowance</span>
                  <span className="font-bold text-gray-700">₹{Number(fixedExpenses.travel || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-xs">
                  <span className="text-gray-500">Insurance</span>
                  <span className="font-bold text-gray-700">₹{Number(fixedExpenses.insurance || 0).toLocaleString('en-IN')}</span>
                </div>
                <div className="flex justify-between items-center text-xs">
                  <span className="text-gray-500">Communication Bill</span>
                  <span className="font-bold text-gray-700">₹{Number(fixedExpenses.communication || 0).toLocaleString('en-IN')}</span>
                </div>
              </div>
              <div className="flex justify-between items-center text-lg font-bold border-t border-gray-100 pt-2 mt-2">
                <span>Total Fixed Deductions</span>
                <span>₹{totalFixed.toLocaleString('en-IN')}</span>
              </div>
            </div>
          </div>
        </section>

        <section className="mb-12">
          <h2 className="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-4 border-l-2 border-emerald-500 pl-3">Daily Transaction Audit Log</h2>
          <table className="w-full text-left">
            <thead>
              <tr className="border-b-2 border-gray-100">
                <th className="py-3 text-[10px] font-black text-gray-400 uppercase tracking-widest">Date</th>
                <th className="py-3 text-[10px] font-black text-gray-400 uppercase tracking-widest">Description / Category</th>
                <th className="py-3 text-[10px] font-black text-gray-400 uppercase tracking-widest text-right">Amount</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-50">
              {monthTxs.map((tx, idx) => (
                <tr key={idx}>
                  <td className="py-3 text-xs font-bold text-gray-400">{new Date(tx.created_at).toLocaleDateString('en-IN', { day: '2-digit', month: 'short' })}</td>
                  <td className="py-3">
                    <div className="flex flex-col">
                      <span className="text-xs font-bold text-[#171717]">{tx.name}</span>
                      <span className="text-[9px] font-bold text-gray-300 uppercase tracking-widest">{tx.category}</span>
                    </div>
                  </td>
                  <td className="py-3 text-sm font-black text-[#171717] text-right">₹{Number(tx.amount).toLocaleString('en-IN')}</td>
                </tr>
              ))}
            </tbody>
          </table>
          {monthTxs.length === 0 && <div className="py-8 text-center text-xs text-gray-300 italic">No transactions recorded for this period.</div>}
        </section>

        <footer className="mt-12 pt-12 border-t-2 border-gray-100">
          <div className="flex justify-between items-start">
            <div className="max-w-sm space-y-4">
              <div className="flex items-center gap-3">
                 <div className="w-12 h-12 bg-[#171717] flex items-center justify-center rounded-lg">
                    <CurrencyInr size={24} weight="bold" className="text-white" />
                 </div>
                 <div className="flex flex-col">
                    <span className="text-lg font-serif font-black tracking-tight">Financial Health Audit</span>
                    <span className="text-[10px] font-bold text-gray-400 uppercase tracking-widest">Generated via Expenditure Engine</span>
                 </div>
              </div>
              <p className="text-[10px] text-gray-400 italic">This statement provides a verified log of income flows and expenditures as recorded by the user. Not valid for tax audits unless supported by original physical receipts and bank statements.</p>
            </div>
            <div className="w-64 space-y-3 bg-gray-50/50 p-6 rounded-2xl">
              <div className="flex justify-between items-center">
                <span className="text-[9px] font-bold text-gray-400 uppercase tracking-widest">Net Cashflow</span>
                <span className={`text-sm font-black ${availableBudget >= 0 ? 'text-emerald-600' : 'text-rose-600'}`}>
                  {availableBudget >= 0 ? '+' : '-'}₹{Math.abs(availableBudget).toLocaleString('en-IN')}
                </span>
              </div>
              <div className="flex justify-between items-center border-t border-gray-200 pt-2">
                <span className="text-[9px] font-bold text-gray-400 uppercase tracking-widest">Closing Balance</span>
                <span className="text-lg font-black text-[#171717]">₹{availableBudget.toLocaleString('en-IN')}</span>
              </div>
            </div>
          </div>
          <div className="mt-12 pt-6 border-t border-gray-50 text-center">
             <p className="text-[8px] font-black text-gray-300 uppercase tracking-[0.4em]">Proprietary Document — Confidential Management Dashboard — © 2026</p>
          </div>
        </footer>
      </div>
    </div>
  )
}
