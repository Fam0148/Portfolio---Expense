# Agent Rules & Guidelines

- **Terminal Error Inspection**: Always inspect the terminal/build logs for errors after making code modifications, and run static type checks (`npx tsc --noEmit`) to ensure zero errors before declaring success.
